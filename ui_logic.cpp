#include "ui_logic.h"

void UILogic::Init() {}

void UILogic::ControlTick(AppState& app, Params& params)
{
    (void)app;
    (void)params;
    // Step 5: controls -> params.targets, set app.ui_dirty if visible change.
}